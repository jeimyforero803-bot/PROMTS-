services:
  - type: web
    name: promts
    runtime: node
    buildCommand: npm install && npm run render-build
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
