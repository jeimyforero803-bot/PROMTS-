services:
  - type: web
    name: promts
    runtime: node
    buildCommand: npm install && echo "GEMINI_API_KEY=$GEMINI_API_KEY" > .env && npm run build
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
